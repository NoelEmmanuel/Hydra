import CoreNode from "./CoreNode";
import ToolNode from "./ToolNode";
import KBNode from "./KBNode";
import ModelNode from "./ModelNode";

export const nodeTypes = {
  core: CoreNode,
  tool: ToolNode,
  kb: KBNode,
  model: ModelNode,
};

export { CoreNode, ToolNode, KBNode, ModelNode };

